# T01 项目骨架

阶段：S0 server
状态：已完成（v0.1，2026-09-26）

## 要做什么
Fastify + TypeScript + SQLite 项目骨架；统一响应格式与全局错误处理；错误码登记在 `server/docs/errors.md`。

## 完成标准
- `npm run dev` 能启动，`GET /health` 返回统一格式
- TypeScript 类型检查通过
- 相关构建、类型检查、测试全部通过
