# T09 后台项目骨架

阶段：S1 admin
状态：待完成

## 要做什么
React + Vite + TypeScript + Tailwind v4 + shadcn/ui，初始化时选择 Base UI 作为底层组件库（不使用 Radix）；主题变量与品牌色；基础布局（侧边栏、顶栏）。

## 完成标准
- `npm run build` 通过
- 组件底层确为 Base UI
- 相关构建、类型检查、测试全部通过
