# 山野咖啡点单平台 · 开发约定

需求原文见 `docs/brief.md`，任务总览见 `PLAN.md`，任务明细在 `todo/` 下。每次开始工作前先读这些文件。

## 目录
```
server/    API 服务（Node.js 24 + TypeScript + Fastify + SQLite）
admin/     后台管理（React 19 + Vite + TS + shadcn/ui（Base UI 底层）+ Tailwind v4 + Recharts）
web/       品牌官网 + 会员中心（Vue 3 + Vite + TS）
miniapp/   点单小程序（uni-app Vue 3，编译到 mp-weixin）
docs/      需求与设计说明
```

## 全局约定
1. API 统一返回 `{"code": 0, "data": ..., "message": "ok"}`；出错时 `code` 非 0，`message` 为中文。错误码登记在 `server/docs/errors.md`。
2. 金额一律用整数“分”存储和传输，只在界面格式化为 `¥xx.xx`。
3. 时间一律用 UTC ISO8601 存储和传输，界面按北京时间显示。
4. 手机号在列表、日志、非本人接口中脱敏为 `138****1234`。
5. 不依赖外部图片或 CDN，插画和图标自绘（SVG / CSS）或使用本地资源。
6. 三个前端共用一套品牌视觉，主题变量写在各端的主题文件中。
7. 每个子项目都有 README：启动命令、默认账号、已知问题。

## 工作流程（每个任务都必须遵守）
1. 任务在 `todo/` 下，一个任务一个文件。按编号顺序做，一次只做一个。
2. 按任务文件中的“完成标准”自查，并运行该子项目的构建 / 类型检查 / 测试，全部通过才算完成。
3. 任务完成后依次执行：
   1. 把任务文件中的 `状态：待完成` 改为 `状态：已完成（vX.Y，YYYY-MM-DD）`；
   2. 用 `git mv` 把任务文件移到 `todo/completed/`；
   3. 在本地提交：`git commit -m "vX.Y 本次工作内容"`；
   4. 推送到远程：`git push`。
4. 版本号：先用 `git log` 查最近一次提交的版本号，把次版本号加 1 作为本次版本号（例如 v0.9 之后是 v0.10）。
5. 提交信息只写一行：版本号 + 一个空格 + 不超过 30 个字的中文说明，写清本次做了什么。不写正文，不写多行。
6. 一次提交只包含一个任务的改动。
7. 不要提交 `node_modules`、构建产物、数据库文件、日志和 `.env`。任何文件、命令和提交信息中都不能出现账号、密码、令牌或 API Key。
8. 推送失败时重试一次；仍然失败，就在汇报中写明原因，继续下一个任务，下次推送时一起推上去。不要修改远程地址，不要配置或输入任何凭据。
9. 当前阶段的任务全部完成后停下，汇报：完成了哪些任务、对应的版本号、怎么启动、还有哪些已知问题。
